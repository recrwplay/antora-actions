# Antora and github actions tests
:no-attribute: 'an attribute'
Just testing workflows, actions, annotations.

## Content to generate xrefs and warnings

include::partial$include-target.adoc[]

I have {no-attribute} that I can use here.

This sentence has been added to see if the pr comment is repeated or updated.

This branch contains a change to test inputs when building from a PR.

// yes
This {attribute} exists.

This new content will be merged by using a PR.
It will then be auto cherry-picked to `main`.`
