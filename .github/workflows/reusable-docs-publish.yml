name: "Publish HTML"

on:
  workflow_call:
    inputs:
      source-dir:
        required: true
        type: string
        default: site/
      docs-artifact:
        required: true
        type: string
        default: docs
      extra-args:
        required: true
        type: string
        default: --dryrun
    secrets:
      aws-s3-bucket: 
        required: true
      aws-s3-id:
        required: true
      aws-s3-secret:
        required: true
      aws-s3-region:
        required: true

jobs:

  aws-sync:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/download-artifact@v2
      with:
        name: ${{ inputs.docs-artifact }}


    - name: Sync to S3
      uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks ${{ inputs.extra-args }}
      env:
        AWS_S3_BUCKET: ${{ secrets.aws-s3-bucket }}
        AWS_ACCESS_KEY_ID: ${{ secrets.aws-s3-id }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.aws-s3-secret }}
        AWS_REGION: ${{ secrets.aws-s3-region }}
        SOURCE_DIR: ${{ inputs.source-dir}}